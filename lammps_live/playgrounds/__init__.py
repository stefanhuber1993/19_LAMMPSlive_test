"""Bundled playgrounds.

Each module here exposes one module-level `PLAYGROUND`. Adding a new one means
adding a file -- nothing else in the codebase changes, and no registry needs
editing (see playground/registry.py, which discovers them).

Your own playgrounds do not have to live here: `--playground path/to/mine.py`
loads one from anywhere.
"""
